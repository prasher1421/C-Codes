#include <iostream>
#include<cmath>
using namespace std;


int main()
{
    float a, areaT,b,h,perimeter,l,sumNterms,n,nTHterm,d,root,c,displacement,v,u;
    a=4*3/2;

    cout<<a<<"\n";
    cout<<"Enter something";
    //cin>>b>>h;


    areaT=(b*h)/2;
    perimeter=2*(l+b);
    sumNterms=n*(n+1)/2;
    nTHterm=a+(n-1)*d;
    root=(-b+sqrt(b*b-4*a*c))/(2*a);
    //displacement=(v*v-u*u)/(2*a);
    displacement=(pow(v,2)-pow(u,2))/(2*a);

    //cout<<"Area is "<<areaT;


    return 0;
}
